const express = require("express");
const router = express.Router();

//importig Controllers
const { contact } = require("../controllers/contact_form");

// importing Middlewares
const {} = require("../middlewares/general");

router.get("");

module.exports = router;
